using System;

namespace study6
{
    class study6
    {
        static void Main(string[] args)
        {
            // //이진수를 정수로 변환
            // Console.Write("2진수를 입력하세요:");
            // string binaryInput = Console.ReadLine();
            // int decimalValue = Convert.ToInt32(binaryInput, 2); //2->10진수 변환

            // //정수를 이진수로 변환
            // string binaryOutput = Convert.ToString(decimalValue, 2);

            // Console.WriteLine($"입력한 2진수 : {binaryOutput}");
            // Console.WriteLine($"변환한 10진수 : {decimalValue}");
            // Console.WriteLine($"다시 2진수로 변환 : {binaryOutput}");

            //var를 사용하여 변수 선언
            // var name = "Alice"; //var 데이터 형식은 컴파일러가 데이터의 형식을 추론한다.
            // var age = 25; //int로 추론
            // var isStudent = true; //bool로 추론

            // Console.WriteLine($"이름 : {name}, 나이 : {age}, 학생 여부 : {isStudent}");
            // int defaultInt = default; //0
            // string defaultString = default; //null
            // bool defaultBool = default; //false

            // Console.WriteLine($"정수 기본값 : {defaultInt}");
            // Console.WriteLine($"문자열 기본값 : {defaultString}");
            // Console.WriteLine($"논리값 기본값 : {defaultBool}");

            // int a = 5, b = 3;
            // int sum = 0;

            // sum = a + b;//산술 연산자 사용
            // Console.WriteLine($"합 : {sum}"); //출력 8
            // sum = a - b;//산술 연산자 사용
            // Console.WriteLine($"빼기 : {sum}"); //출력 8
            // sum = a * b;//산술 연산자 사용
            // Console.WriteLine($"곱 : {sum}"); //출력 8
            // sum = a / b;//산술 연산자 사용
            // Console.WriteLine($"나눈 몫 : {sum}"); //출력 8
            // int c = 10, d = 3;
            // sum = c % d;
            // Console.WriteLine($"나머지 : {sum}");

            // int number = 7;
            // int sum = 0;

            // sum = number % 2; //0이면 짝수,1이면 홀수

            // Console.WriteLine($"짝수 홀수 판별 : {sum}");

            // bool isEqual = false; // 거짓 0
            // int a = 5 , b = 5;

            // //관계형 연산자
            // isEqual = (a == b); //a랑 b랑 같은지?
            // Console.WriteLine(isEqual);

            

        }
    }
}